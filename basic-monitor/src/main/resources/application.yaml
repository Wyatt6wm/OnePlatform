spring:
  application:
    name: basic-monitor
  profiles:
    active: run

server:
  port: 8001

# ?????????????????????/actuator/*?
management:
  endpoints:
    web:
      exposure:
        include: "*"

sys:
  op-registry:
    host: localhost
    port: 8500
    domain: ${sys.op-registry.host}:${sys.op-registry.port}
  op-monitor:
    host: localhost
    port: 8001
    domain: ${sys.op-monitor.host}:${sys.op-monitor.port}
  op-user:
    host: localhost
    port: 8002
    domain: ${sys.op-user.host}:${sys.op-user.port}